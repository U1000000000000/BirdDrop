# Production environment — loaded automatically by Vite for production builds.
# Vercel: add this as Project → Settings → Environment Variables as well.

# WebSocket must connect directly to Render backend (Vercel can't proxy WebSockets)
VITE_WS_URL=wss://rendersal.onrender.com/birddrop
